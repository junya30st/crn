= render "shared/header"

.signup
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "devise/shared/error_messages", resource: resource
    .signup--wrapper
      .signup--wrapper--fix
        .signup--wrapper__fix--title
          ユーザー新規登録
        .signup--wrapper__fix--contents
          .signup-form
            = f.label :name, '名前', class: 'label-up'
            %span{class: 'form-must'} 必須
            %br
            = f.text_field :name, autofocus: true, placeholder: "ニックネーム（5文字以上）", class: "form-box"
          .signup-form
            =f.label :email, "メールアドレス", class: 'label-up'
            %span{class: 'form-must'} 必須
            %br
            = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "例) xxx@xxx.com", class: "form-box"
          .signup-form
            = f.label :password, "パスワード", class: 'label-up'
            %span{class: 'form-must'} 必須
            %br
            = f.password_field :password, autocomplete: "new-password", class: "form-box"
          .signup-form
            = f.label :password_confirmation, "パスワード確認", class: 'label-up'
            %span{class: 'form-must'} 必須
            %br
            = f.password_field :password_confirmation, autocomplete: "new-password", class: "form-box"
          .button-register
            = f.submit "登録する", class: "submit-button"
          
          .sns-registration
            = link_to user_twitter_omniauth_authorize_path do
              .sns-registration__twitter
                .fab.fa-twitter
                Twitterアカウントで登録


= render "shared/footer"

-# %h2 Sign up
-# = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
-#   = render "devise/shared/error_messages", resource: resource
-#   .field
-#     = f.label :email
-#     %br/
-#     = f.email_field :email, autofocus: true, autocomplete: "email"
-#   .field
-#     = f.label :password
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#     %br/
-#     = f.password_field :password, autocomplete: "new-password"
-#   .field
-#     = f.label :password_confirmation
-#     %br/
-#     = f.password_field :password_confirmation, autocomplete: "new-password"
-#   .actions
-#     = f.submit "Sign up"
-# = render "devise/shared/links"